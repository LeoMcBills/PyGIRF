# Publish to PyPI when you push a version tag (e.g. v0.1.1).
# One-time setup: register this workflow as a "trusted publisher" on PyPI — see README.

name: Publish to PyPI

on:
  push:
    tags:
      - "v*"

permissions:
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write # required for PyPI trusted publishing (OIDC)

    steps:
      - uses: actions/checkout@v4

      # Tag pushes must point to a commit whose pyproject version is NEW on PyPI (not already uploaded).
      - name: Version to build (must not duplicate an existing PyPI release)
        run: grep -E '^version\s*=' pyproject.toml

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install build tools
        run: pip install build twine

      - name: Build sdist and wheel
        run: python -m build

      - name: Check distributions
        run: twine check dist/*

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          packages-dir: dist/
